Scan the codebase for true bugs only — logic errors, race conditions, unhandled edge cases, incorrect API usage, null/undefined access, off-by-one errors, and broken error handling. Ignore the hardcoded API key security issue. Ignore performance, style, naming, and formatting. For each bug found: describe the incorrect behavior, explain the user-facing impact, plan the fix, then implement it. Start with the most critical bugs first.
