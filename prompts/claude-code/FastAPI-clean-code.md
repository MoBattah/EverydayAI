Audit this FastAPI Python codebase for structural and architectural quality with the goal of refactoring it into a more professional, maintainable production codebase. Focus on backend architecture, separation of concerns, modularity, dependency boundaries, configuration management, and operational hygiene. Apply principles consistent with well-structured Python services and Twelve-Factor App concepts where relevant.

Specifically, look for issues such as: god modules or oversized files, route handlers that contain business logic, weak separation between API, service, domain, and data-access layers, direct database access from controllers, duplicated logic, poor dependency injection patterns, hidden global state, brittle coupling across modules, configuration or secrets handled incorrectly, inconsistent error handling, weak schema boundaries between Pydantic models and persistence models, background tasks or async code structured unsafely, and utility modules that have become catch-alls.

Ignore code style, naming, and formatting unless they are contributing to a deeper architectural problem. Do not spend time on minor cleanup that does not materially improve maintainability, testability, scalability, or operability.
